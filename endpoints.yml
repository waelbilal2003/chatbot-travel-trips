action_endpoint:
  url: "http://0.0.0.0:5055/webhook"  # نقطة النهاية لإجراءات Rasa

tracker_store:
  type: SQL
  dialect: postgresql
  url: postgresql://postgres:${DB_PASSWORD}@db.yhwqonkzygwxibntescs.supabase.co:5432/postgres
  db: postgres

cors:
  - "*"                                  # السماح بجميع مصادر CORS للتطبيق