action_endpoint:
  url: "https://actions-chatbot-travel-trips.onrender.com/webhook"  # ✔️ الاتصال الداخلي بين الخوادم

lock_store:
  type: in_memory  # ✔️ التخزين في الذاكرة

tracker_store:
  type: in_memory  # ✔️ لا حاجة لقاعدة بيانات

cors:
  - "*"  # ✔️ السماح بجميع المصادر (للتطوير)