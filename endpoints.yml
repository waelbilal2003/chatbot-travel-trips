action_endpoint:
  url: "http://0.0.0.0:$ACTION_PORT/webhook"  # ✔️ الاتصال الداخلي بين الخوادم

lock_store:
  type: in_memory  # ✔️ التخزين في الذاكرة

tracker_store:
  type: in_memory  # ✔️ لا حاجة لقاعدة بيانات

cors:
  - "*"  # ✔️ السماح بجميع المصادر (للتطوير)